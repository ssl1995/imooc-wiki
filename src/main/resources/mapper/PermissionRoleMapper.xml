<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiawa.wiki.mapper.PermissionRoleMapper">
    <resultMap id="BaseResultMap" type="com.jiawa.wiki.domain.Role">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="role_name" jdbcType="VARCHAR" property="roleName" />
        <result column="permission_desc" jdbcType="VARCHAR" property="permissionDesc" />
        <result column="is_delete" jdbcType="TINYINT" property="isDelete" />
        <result column="create_time" jdbcType="BIGINT" property="createTime" />
        <result column="update_time" jdbcType="BIGINT" property="updateTime" />
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, role_name, permission_desc, is_delete, create_time, update_time
        FROM per_role
        WHERE is_delete = 0
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, role_name, permission_desc, is_delete, create_time, update_time
        FROM per_role
        WHERE id = #{id}
          AND is_delete = 0
    </select>
</mapper>
