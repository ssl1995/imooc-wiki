<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jiawa.wiki.mapper.ImageInfoMapper">
    <resultMap id="BaseResultMap" type="com.jiawa.wiki.domain.Image">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="tree_id" jdbcType="BIGINT" property="treeId" />
        <result column="image_path" jdbcType="VARCHAR" property="imagePath" />
        <result column="hash_code" jdbcType="VARCHAR" property="hashCode" />
        <result column="hash_bits" jdbcType="BINARY" property="hashBits" />
        <result column="is_delete" jdbcType="TINYINT" property="isDelete" />
        <result column="create_time" jdbcType="BIGINT" property="createTime" />
        <result column="update_time" jdbcType="BIGINT" property="updateTime" />
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, tree_id, image_path, hash_code, hash_bits, is_delete, create_time, update_time
        FROM image
        WHERE is_delete = 0
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, tree_id, image_path, hash_code, hash_bits, is_delete, create_time, update_time
        FROM image
        WHERE id = #{id}
          AND is_delete = 0
    </select>

    <select id="selectByTreeId" resultMap="BaseResultMap">
        SELECT id, tree_id, image_path, hash_code, hash_bits, is_delete, create_time, update_time
        FROM image
        WHERE tree_id = #{treeId}
          AND is_delete = 0
        LIMIT 1
    </select>
</mapper>
