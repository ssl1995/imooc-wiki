server:
  port: 8099
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wiki-dev?characterEncoding=UTF8&autoReconnect=true&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
#    host: r-uf6ljbcdaxobsifyctpd.redis.rds.aliyuncs.com
#    password: Redis000
#    port: 6379
    host: localhost
    port: 6379


logging:
  level:
    com:
      jiawa:
        wiki:
          mapper: trace

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml

tree:
  image-root: algorithm/data_samples/bfath_demo

# DHLAM 模型推理服务配置
dhlam:
  api:
    url: http://localhost:5000     # Python Flask 推理服务地址，空则禁用
  demo-mode: true                  # true=走模拟逻辑，false=HTTP调用真实Python模型

test:
  hello: Hello1
