================================================================================
公式说明变量缺失对照表（Reviewed版）
论文：5200090_宋声林_融合图像与地理位置元数据的古树名木多模态哈希检索系统研建_33.docx
审查日期：2026-04-15
说明：本对照表经全文逐条核对后生成，包含已有对照表全部内容，并补充了遗漏的公式问题。
      所有建议的变量符号均基于正文上下文推断，修改前请务必与Word文档中的实际公式符号核对一致。
================================================================================

【公式(2-1)】行79
--------------------------------------------------------------------------------
[当前文本]
其中，代表第i个输出特征图；代表单一卷积核中第k通道的数据；代表输入特征图中第k个通道的数据；是对第i个输出特征的偏移量。

[建议修改]
其中，y_i代表第i个输出特征图；w_{i,k}代表单一卷积核中第k通道的数据；x_k代表输入特征图中第k个通道的数据；b_i是对第i个输出特征的偏移量。

[备注] 请与正文公式(2-1)中的实际符号保持一致。若公式中使用的是Y^i、W_{i,k}、X^k、B^i等，请相应调整。


【公式(2-2)】行113（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，表示哈希函数，表示欧氏距离，为间隔超参数。

[建议修改]
其中，h(·)表示哈希函数，d(·,·)表示欧氏距离，m为间隔超参数。

[备注] 新增发现。若正文公式中使用的是f(·)、‖·‖_2、α等符号，请按实际调整。Triplet Loss标准形式中，锚点a经哈希函数映射后通常写作h(a)。


【公式(2-3)】行120
--------------------------------------------------------------------------------
[当前文本]
其中，表示样本对是否相似，表示距离度量，m是不相似样本阈值。

[建议修改]
其中，y∈{0,1}表示样本对是否相似，d(·,·)表示距离度量，m是不相似样本阈值。

[备注] y为样本对标签，d为距离函数，m为margin间隔参数。


【公式(2-4)】行127（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，f为输入特征向量，为权重矩阵，为偏置向量，h为连续哈希码。

[建议修改]
其中，f为输入特征向量，W为权重矩阵，b为偏置向量，h为连续哈希码。

[备注] 新增发现。公式说明中权重矩阵和偏置向量缺少变量符号。请与正文公式(2-4)中的实际符号（如W_h、b_h等）保持一致。


【公式(2-6)】行137
--------------------------------------------------------------------------------
[当前文本]
其中，为输入特征，为残差映射，为输出特征。残差连接通过跳过连接（Skip Connection）将输入直接加到输出上，有效缓解了深层网络的梯度消失问题。

[建议修改]
其中，x为输入特征，F(x)为残差映射，H(x)为输出特征。残差连接通过跳过连接（Skip Connection）将输入直接加到输出上，有效缓解了深层网络的梯度消失问题。

[备注] 若正文公式中使用的是x、y、F(x,y)等，请按实际符号调整。


【公式(2-7)】行142（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，Q、K、V分别为查询、键和值矩阵，为键的维度。

[建议修改]
其中，Q、K、V分别为查询、键和值矩阵，d_k为键的维度。

[备注] 新增发现。标准自注意力公式分母为√d_k，说明中的"dk"应规范为下标形式"d_k"，与公式中的维度符号对应。


【公式(2-9)(2-10)(2-11)】行155（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，（lat,lon）∈R²为输入的位置坐标向量；W₁∈R^(128×2)、W₂∈R^(128×128)、W₃∈R^(256×128)为各层权重矩阵；b₁、b₂、b₃为偏置向量；h₁、h₂为中间层特征；∈R^256为输出的位置特征向量。

[建议修改]
其中，（lat,lon）∈R²为输入的位置坐标向量；W₁∈R^(128×2)、W₂∈R^(128×128)、W₃∈R^(256×128)为各层权重矩阵；b₁、b₂、b₃为偏置向量；h₁、h₂为中间层特征；f_geo∈R^256为输出的位置特征向量。

[备注] 新增发现。行155与行263（公式3-4/3-5/3-6）为同一组公式的重复引用，符号说明应保持一致。


【公式(2-12)】行199（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，图像表示与经过双线性池化操作得到的融合表示。

[建议修改]
其中，v为图像表示，u为位置表示，z为经过双线性池化操作得到的融合表示。

[备注] 新增发现。根据后文"图像特征向量v与位置特征向量u的外积"推断变量符号。若正文公式中使用的是x^I、x^L、y等符号，请按实际调整。


【公式(3-1)】行245
--------------------------------------------------------------------------------
[当前文本]
其中， 为输入特征；为残差映射；为输出特征。

[建议修改]
其中，x为输入特征；F(x)为残差映射；y为输出特征。

[备注] 修正原备注表述：行245的公式(3-1)与行137的公式(2-6)为相同形式的残差块公式，符号说明应保持一致，但二者分属不同章节，请勿合并公式编号。


【公式(3-2)】行250（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，Q、K、V分别为查询、键、值矩阵；dk为维度缩放因子。

[建议修改]
其中，Q、K、V分别为查询、键、值矩阵；d_k为维度缩放因子。

[备注] 新增发现。dk连写不规范，建议改为下标形式"d_k"，与标准自注意力公式及公式中的分母√d_k保持一致。


【公式(3-4)(3-5)(3-6)】行263
--------------------------------------------------------------------------------
[当前文本]
其中，为输入的位置坐标向量；、、为各层权重矩阵；b₁、b₂、b₃为偏置向量；h₁、h₂为中间层特征；为输出的位置特征向量。

[建议修改]
其中，(lat,lon)∈R²为输入的位置坐标向量；W₁∈R^(128×2)、W₂∈R^(128×128)、W₃∈R^(256×128)为各层权重矩阵；b₁、b₂、b₃为偏置向量；h₁、h₂为中间层特征；f_geo∈R^256为输出的位置特征向量。

[备注] 前一行(行261-262)有完整公式，本行说明应与公式中的W₁/W₂/W₃和f_geo对应。


【公式(3-7)】行272
--------------------------------------------------------------------------------
[当前文本]
其中，为图像特征向量，维度取决于特征网络；为位置特征向量。

[建议修改]
其中，v_img∈R^d为图像特征向量，维度取决于特征网络；v_loc∈R^256为位置特征向量。

[备注] d为图像特征维度（如512或768），v_loc为位置特征。若公式中使用v_i和v_l等符号，请按实际调整。


【公式(3-8)(3-9)】行277
--------------------------------------------------------------------------------
[当前文本]
其中，为Sigmoid激活函数；为注意力权重矩阵；和为投影矩阵，将不同维度的特征映射至统一空间；α∈(0,1)为自适应融合权重。

[建议修改]
其中，σ(·)为Sigmoid激活函数；W_α∈R^(d×d)为注意力权重矩阵；W_v和W_u为投影矩阵，将不同维度的特征映射至统一空间；α∈(0,1)为自适应融合权重。

[备注] σ或sigmoid均可，但需明确写出变量符号；W_α、W_v、W_u请与公式中投影矩阵的符号保持一致。


【公式(3-12)】行290
--------------------------------------------------------------------------------
[当前文本]
其中，为量化损失；L为哈希码长度（L=128）；为第i位连续值输出；sign()为对应的离散哈希码。该损失引导网络生成"接近二进制"的连续输出，有效缓解连续松弛引入的量化误差。哈希码长度L可配置为64位、128位或256位。较短哈希码存储开销更低但表征能力有限；较长哈希码检索精度更高但增加存储与计算负担。经实验验证，128位哈希码在检索精度、存储开销与计算效率之间达到最佳平衡，故作为默认配置。

[建议修改]
其中，L_q为量化损失；L为哈希码长度（L=128）；h_i为第i位连续值输出；sign(h_i)为对应的离散哈希码。该损失引导网络生成"接近二进制"的连续输出，有效缓解连续松弛引入的量化误差。哈希码长度L可配置为64位、128位或256位。较短哈希码存储开销更低但表征能力有限；较长哈希码检索精度更高但增加存储与计算负担。经实验验证，128位哈希码在检索精度、存储开销与计算效率之间达到最佳平衡，故作为默认配置。

[备注] 量化损失符号常用L_q、Q或L_quant，请与正文公式保持一致。sign()中的点或h_i需补充。


【公式(3-13)】行298
--------------------------------------------------------------------------------
[当前文本]
其中，为锚点与正样本距离；为锚点与负样本距离；为间隔参数，控制正负样本对的最小距离差，本文设置=0.5。

[建议修改]
其中，d(a,p)为锚点与正样本距离；d(a,n)为锚点与负样本距离；m为间隔参数，控制正负样本对的最小距离差，本文设置m=0.5。

[备注] 请注意"本文设置=0.5"缺少变量名，必须改为"本文设置m=0.5"或"本文设置α=0.5"。


【公式(3-14)】行303（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，∈{0,1}为样本对标签。

[建议修改]
其中，y∈{0,1}为样本对标签。

[备注] 新增发现。公式(3-14)的说明中样本对标签符号缺失。请与公式(2-3)和(3-15)保持一致，统一使用y表示样本对相似性标签。


【公式(3-15)】行306
--------------------------------------------------------------------------------
[当前文本]
其中，为样本对标签；为样本对欧氏距离。

[建议修改]
其中，y∈{0,1}为样本对标签；d为样本对欧氏距离。

[备注] 与公式(2-3)保持一致，y为标签，d为距离。


【公式(3-16)】行313（新增）
--------------------------------------------------------------------------------
[当前文本]
其中，即负样本虽比正样本远，但未满足间隔约束，位于决策边界附近。

[建议修改]
其中，d(a,p)为锚点与正样本距离，d(a,n)为锚点与负样本距离，m为间隔参数，即负样本虽比正样本远，但未满足间隔约束，位于决策边界附近。

[备注] 新增发现。公式(3-16)的"其中"后缺少变量说明，导致句子无主语。若正文公式使用了集合符号（如H、N_hard或T_hard）表示难例集合，请补充相应符号及其说明。


【公式(3-18)】行324
--------------------------------------------------------------------------------
[当前文本]
其中，为查询样本的连续值哈希码；为库样本的连续值哈希码；为余弦相似度，取值范围为[−1, 1]。余弦相似度通过度量两向量夹角的余弦值反映语义相似程度，值越大表示夹角越小、相似度高。

[建议修改]
其中，q为查询样本的连续值哈希码；g为库样本的连续值哈希码；sim(q,g)为余弦相似度，取值范围为[−1, 1]。余弦相似度通过度量两向量夹角的余弦值反映语义相似程度，值越大表示夹角越小、相似度高。

[备注] q(query)和g(gallery)为检索领域通用符号，也可用h_q和h_g。sim或cos(θ)均可，请与公式保持一致。


【公式(3-19)】行328
--------------------------------------------------------------------------------
[当前文本]
其中，,为第位二进制哈希码；L为哈希码长度；表示按位异或（XOR）运算。为归一化汉明距离，取值范围为[0, 1]，值越小表示相似度越高。得益于二进制的位运算特性，汉明距离计算可通过CPU的XOR和POPCNT指令在常数时间内完成，适合大规模数据的快速筛选。

[建议修改]
其中，b_i^q,b_i^g∈{+1,-1}为第i位二进制哈希码；L为哈希码长度；⊕表示按位异或（XOR）运算。d_H为归一化汉明距离，取值范围为[0, 1]，值越小表示相似度越高。得益于二进制的位运算特性，汉明距离计算可通过CPU的XOR和POPCNT指令在常数时间内完成，适合大规模数据的快速筛选。

[备注] 本句存在多处缺失：① "其中，,为第位"缺少变量、缺少下标i；② "表示按位异或"缺少运算符符号⊕；③ "为归一化汉明距离"缺少d_H符号。


================================================================================
通用检查建议
================================================================================
1. 全文搜索关键词："其中，为"、"其中，代表"、"其中，表示"，检查是否还有其他遗漏。
2. 公式说明中的变量符号必须与正文中公式里的符号完全一致（包括大小写、上下标、粗斜体）。
3. 设置类描述（如"本文设置=0.5"）必须补全变量名（如"本文设置m=0.5"）。
4. 运算符符号（⊕、∈、∑、√等）不应在说明文字中缺失或写错。
5. 同一公式在文中多次出现时（如公式2-6与3-1、公式2-9与3-4等），其变量说明应保持一致。
6. 下标与上标需规范书写：如"dk"应写为"d_k"，"b_i^q"的上下标需与公式一致。
7. 修改完成后，建议再次通读所有公式及其紧跟的说明文字，确保每个被说明的量前都有明确的符号。
================================================================================

================================================================================
本次审查对原对照表的修正说明
================================================================================
1. 增补公式：新增7处遗漏问题，涉及公式(2-2)、(2-4)、(2-7)、(2-9)(2-10)(2-11)、(2-12)、(3-2)、(3-14)、(3-16)。
2. 修正备注：公式(3-1)原备注"行245与行137为同一公式"表述不够严谨，已修正为"相同形式的残差块公式，符号说明应保持一致"。
3. 规范提醒：公式(3-2)和(2-7)中的"dk"连写问题已纳入，建议统一规范为下标"d_k"。
4. 已有条目复核：原对照表中的(2-1)、(2-3)、(2-6)、(3-4)(3-5)(3-6)、(3-7)、(3-8)(3-9)、(3-12)、(3-13)、(3-15)、(3-18)、(3-19)等条目经核对后内容正确，全部保留。
================================================================================
