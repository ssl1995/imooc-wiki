3.2.5多模态融合策略
古树名木图像检索任务中，除图像信息外，地理位置元数据（经纬度坐标）也是重要的辅助特征。同种古树往往分布在相似的地理区域内，地理位置元数据能够有效补充视觉特征的不足，特别是在图像质量较差或视觉相似树种的判别场景中。本节介绍地理位置元数据的编码方式及多模态特征融合策略。
（1）地理位置元数据编码
地理位置元数据以二维位置坐标(lat, lon)的形式输入，采用轻量级MLP进行非线性编码。位置编码器数据表达式如公式（3-16）、公式（3-17）和公式（3-18）所示：
	h1=ReLU(W1⋅[lat;lon]+b1)   	（3-16）
	h2=ReLU(W2⋅h1+b2)     	（3-17）
	fgeo=W3⋅h2+b3 	 （3-18）
其中，[lat;lon]∈R²为位置坐标向量；W1∈R^(128×2)、W2∈R^(128×128)、
W3∈R^(256×128)为各层权重矩阵；b1、b2、b3为偏置向量；h1、h2为中间层特征；ReLU为激活函数；fgeo∈R^256为输出的位置特征向量。
选择256维输出是为了与图像特征维度形成合理比例，避免单一模态在融合过程中被淹没。
（2）特征融合方法
本节模型采用以特征拼接为主、向量融合为备选的双策略融合方案。特征拼接是主模型采用的方法，将图像特征向量与位置特征向量按通道维度进行拼接，构建包含双模态信息的融合特征向量，其数学表达式如公式（3-19）所示：
	ffused=[fimg;fgeo]  	（3-19）
其中，fimg为图像特征向量，维度取决于特征网络；fgeo为位置特征向量。
拼接后的融合特征通过全连接层映射至目标维度（如512维），作为哈希层的输入。特征拼接的优势在于计算简单、无额外训练参数，且完整保留了两个模态的信息。
为验证不同融合策略的效果，向量融合是备选方案，本节同时实现基于自适应权重机制的向量融合方法。该方法通过计算模态间的注意力权重，动态调整图像特征与位置特征的相对贡献，数学表达式如公式（3-20）所示：
	α=σ(Watt⋅[fimg;fgeo]+batt)    	（3-20）
	ffused=α⋅Wimg⋅fimg+(1−α)⋅Wgeo⋅fgeo	  （3-21）
其中，σ为Sigmoid激活函数；Watt为注意力权重矩阵；Wimg和Wgeo为投影矩阵，将不同维度的特征映射至统一空间；α∈(0,1)为自适应融合权重。
向量融合通过自适应机制使模型能够根据输入样本动态决定不同模态的重要性。